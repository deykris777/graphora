{
  "name": "graphora",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace apps/server\" \"npm run dev --workspace apps/web\"",
    "build": "npm run build --workspace apps/server && npm run build --workspace apps/web && npm run build --workspace packages/sdk",
    "start": "npm run start --workspace apps/server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
